{
  "name": "vobiz-freshdesk-calling",
  "version": "1.0.0",
  "description": "Vobiz calling panel for Freshdesk",
  "private": true,
  "license": "MIT",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "mock-backend": "node mock-backend/server.js",
    "fdk-unit-test": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
